# Ch-3

Bound Variables: 在其作用域(Scopes)里可以更换其名字，而不影响过程结果

## Stream Processing

将程序看作是数据滤波器，逐层向下进行，实现思路有两种

1. 将数据流整体作为处理对象
2. 将数据流中的单个元素作为处理对象

Delay：重新安排计算顺序，临时存储计算结果

Memorization：记录计算结果，如果下次的输入参数相同，则直接读取结果（利用空间换时间）

赋值运算及没有赋值运算的纯函数式语言

纯函数式语言的缺陷：没有局部状态存储（因为没有赋值运算），导致无法处理并行程序中遇到的**同步**问题